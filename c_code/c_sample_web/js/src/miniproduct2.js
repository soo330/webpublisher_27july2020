(function($){
  var miniP = $('#miniProduct');
  var proWrap = miniP.find('.product_wrap');
  var backImg = proWrap.find('.product2');
  var backL = backImg.find('li');

  var slideIndi = miniP('.slide_indi'); //div
  var slideIndi2Ul = slideIndi.find('.indicator2');
  
  var backLLast = backL.eq(-1).clone();
  backImg.prepend(backLLst);
  var 
})(jQuery);